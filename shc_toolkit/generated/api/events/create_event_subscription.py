from http import HTTPStatus
from typing import Any

import httpx

from ... import errors
from ...client import AuthenticatedClient, Client
from ...models.create_event_subscription_response_200 import (
    CreateEventSubscriptionResponse200,
)
from ...models.create_event_subscription_response_201 import (
    CreateEventSubscriptionResponse201,
)
from ...models.event_subscription_create_request import EventSubscriptionCreateRequest
from ...models.problem import Problem
from ...types import Response


def _get_kwargs(
    *,
    body: EventSubscriptionCreateRequest,
    idempotency_key: str,
) -> dict[str, Any]:
    headers: dict[str, Any] = {}
    headers["Idempotency-Key"] = idempotency_key

    _kwargs: dict[str, Any] = {
        "method": "post",
        "url": "/event-subscriptions",
    }

    _kwargs["json"] = body.to_dict()

    headers["Content-Type"] = "application/json"

    _kwargs["headers"] = headers
    return _kwargs


def _parse_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> (
    CreateEventSubscriptionResponse200
    | CreateEventSubscriptionResponse201
    | Problem
    | None
):
    if response.status_code == 200:
        response_200 = CreateEventSubscriptionResponse200.from_dict(response.json())

        return response_200

    if response.status_code == 201:
        response_201 = CreateEventSubscriptionResponse201.from_dict(response.json())

        return response_201

    if response.status_code == 400:
        response_400 = Problem.from_dict(response.json())

        return response_400

    if response.status_code == 401:
        response_401 = Problem.from_dict(response.json())

        return response_401

    if response.status_code == 403:
        response_403 = Problem.from_dict(response.json())

        return response_403

    if response.status_code == 404:
        response_404 = Problem.from_dict(response.json())

        return response_404

    if response.status_code == 405:
        response_405 = Problem.from_dict(response.json())

        return response_405

    if response.status_code == 409:
        response_409 = Problem.from_dict(response.json())

        return response_409

    if response.status_code == 413:
        response_413 = Problem.from_dict(response.json())

        return response_413

    if response.status_code == 415:
        response_415 = Problem.from_dict(response.json())

        return response_415

    if response.status_code == 422:
        response_422 = Problem.from_dict(response.json())

        return response_422

    if response.status_code == 429:
        response_429 = Problem.from_dict(response.json())

        return response_429

    if response.status_code == 503:
        response_503 = Problem.from_dict(response.json())

        return response_503

    if client.raise_on_unexpected_status:
        raise errors.UnexpectedStatus(response.status_code, response.content)
    else:
        return None


def _build_response(
    *, client: AuthenticatedClient | Client, response: httpx.Response
) -> Response[
    CreateEventSubscriptionResponse200 | CreateEventSubscriptionResponse201 | Problem
]:
    return Response(
        status_code=HTTPStatus(response.status_code),
        content=response.content,
        headers=response.headers,
        parsed=_parse_response(client=client, response=response),
    )


def sync_detailed(
    *,
    client: AuthenticatedClient | Client,
    body: EventSubscriptionCreateRequest,
    idempotency_key: str,
) -> Response[
    CreateEventSubscriptionResponse200 | CreateEventSubscriptionResponse201 | Problem
]:
    """Create event webhook subscription

     Registers a customer-owned webhook target for matching events from GET /events. The target must be
    standard-port HTTPS on a public host. Each delivery body is a CloudEvents 1.0 JSON event with
    extension attributes shcdeliveryid, shcsubscriptionid, shcattempt, and shcwebhookversion. Verify
    X-SHC-Webhook-Signature by computing HMAC-SHA256 over the exact raw request body bytes with the
    subscription secret and comparing to the sha256=<hex> header using a constant-time comparison.

    Args:
        idempotency_key (str):
        body (EventSubscriptionCreateRequest): Register an asynchronous webhook subscription for
            the existing customer event feed. The signing secret is generated by the server and
            returned only in the first 201 response, never in replay or read responses. Example:
            {'url': 'https://hooks.customer-domain.com/shc/webhooks', 'eventTypes':
            ['service.lifecycle.*', 'billing.invoice.*']}.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[CreateEventSubscriptionResponse200 | CreateEventSubscriptionResponse201 | Problem]
    """

    kwargs = _get_kwargs(
        body=body,
        idempotency_key=idempotency_key,
    )

    response = client.get_httpx_client().request(
        **kwargs,
    )

    return _build_response(client=client, response=response)


def sync(
    *,
    client: AuthenticatedClient | Client,
    body: EventSubscriptionCreateRequest,
    idempotency_key: str,
) -> (
    CreateEventSubscriptionResponse200
    | CreateEventSubscriptionResponse201
    | Problem
    | None
):
    """Create event webhook subscription

     Registers a customer-owned webhook target for matching events from GET /events. The target must be
    standard-port HTTPS on a public host. Each delivery body is a CloudEvents 1.0 JSON event with
    extension attributes shcdeliveryid, shcsubscriptionid, shcattempt, and shcwebhookversion. Verify
    X-SHC-Webhook-Signature by computing HMAC-SHA256 over the exact raw request body bytes with the
    subscription secret and comparing to the sha256=<hex> header using a constant-time comparison.

    Args:
        idempotency_key (str):
        body (EventSubscriptionCreateRequest): Register an asynchronous webhook subscription for
            the existing customer event feed. The signing secret is generated by the server and
            returned only in the first 201 response, never in replay or read responses. Example:
            {'url': 'https://hooks.customer-domain.com/shc/webhooks', 'eventTypes':
            ['service.lifecycle.*', 'billing.invoice.*']}.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        CreateEventSubscriptionResponse200 | CreateEventSubscriptionResponse201 | Problem
    """

    return sync_detailed(
        client=client,
        body=body,
        idempotency_key=idempotency_key,
    ).parsed


async def asyncio_detailed(
    *,
    client: AuthenticatedClient | Client,
    body: EventSubscriptionCreateRequest,
    idempotency_key: str,
) -> Response[
    CreateEventSubscriptionResponse200 | CreateEventSubscriptionResponse201 | Problem
]:
    """Create event webhook subscription

     Registers a customer-owned webhook target for matching events from GET /events. The target must be
    standard-port HTTPS on a public host. Each delivery body is a CloudEvents 1.0 JSON event with
    extension attributes shcdeliveryid, shcsubscriptionid, shcattempt, and shcwebhookversion. Verify
    X-SHC-Webhook-Signature by computing HMAC-SHA256 over the exact raw request body bytes with the
    subscription secret and comparing to the sha256=<hex> header using a constant-time comparison.

    Args:
        idempotency_key (str):
        body (EventSubscriptionCreateRequest): Register an asynchronous webhook subscription for
            the existing customer event feed. The signing secret is generated by the server and
            returned only in the first 201 response, never in replay or read responses. Example:
            {'url': 'https://hooks.customer-domain.com/shc/webhooks', 'eventTypes':
            ['service.lifecycle.*', 'billing.invoice.*']}.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        Response[CreateEventSubscriptionResponse200 | CreateEventSubscriptionResponse201 | Problem]
    """

    kwargs = _get_kwargs(
        body=body,
        idempotency_key=idempotency_key,
    )

    response = await client.get_async_httpx_client().request(**kwargs)

    return _build_response(client=client, response=response)


async def asyncio(
    *,
    client: AuthenticatedClient | Client,
    body: EventSubscriptionCreateRequest,
    idempotency_key: str,
) -> (
    CreateEventSubscriptionResponse200
    | CreateEventSubscriptionResponse201
    | Problem
    | None
):
    """Create event webhook subscription

     Registers a customer-owned webhook target for matching events from GET /events. The target must be
    standard-port HTTPS on a public host. Each delivery body is a CloudEvents 1.0 JSON event with
    extension attributes shcdeliveryid, shcsubscriptionid, shcattempt, and shcwebhookversion. Verify
    X-SHC-Webhook-Signature by computing HMAC-SHA256 over the exact raw request body bytes with the
    subscription secret and comparing to the sha256=<hex> header using a constant-time comparison.

    Args:
        idempotency_key (str):
        body (EventSubscriptionCreateRequest): Register an asynchronous webhook subscription for
            the existing customer event feed. The signing secret is generated by the server and
            returned only in the first 201 response, never in replay or read responses. Example:
            {'url': 'https://hooks.customer-domain.com/shc/webhooks', 'eventTypes':
            ['service.lifecycle.*', 'billing.invoice.*']}.

    Raises:
        errors.UnexpectedStatus: If the server returns an undocumented status code and Client.raise_on_unexpected_status is True.
        httpx.TimeoutException: If the request takes longer than Client.timeout.

    Returns:
        CreateEventSubscriptionResponse200 | CreateEventSubscriptionResponse201 | Problem
    """

    return (
        await asyncio_detailed(
            client=client,
            body=body,
            idempotency_key=idempotency_key,
        )
    ).parsed
