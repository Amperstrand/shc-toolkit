.venv/
*.egg-info/
__pycache__/
dist/
build/
*.pyc
.env

# Benchmark results (private)
benchmark_results/
bench-*

# Dev VPS plan config (shared/private packages)
dev-vps-plans.json

# Tollgate build artifacts
tollgate/cashu-tollgate

# OpenCode agent state
.omo/

# Local notes and audit artifacts
notes/

# Debug screenshots
*.png

# Playwright MCP session artifacts (logs, snapshots)
.playwright-mcp/

# Console diagnostic screenshots (session artifacts, not source)
console-*.png
diag-*.png
popout-*.png
r[0-9]-*.png

# Compiled tollgate binary (cross-compiled locally)
tollgate/cashu-tollgate-linux-amd64
uv.lock
