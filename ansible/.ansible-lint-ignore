# This file contains ignores rule violations for ansible-lint.
# Only the 6 jinja[invalid] false positives remain — they're shell
# heredoc syntax that ansible-lint's jinja parser doesn't grok but
# Ansible itself handles fine. The 5 real findings (no-changed-when,
# var-naming, yaml[line-length]) were fixed in commit history.
roles/nodns/tasks/main.yml jinja[invalid]
roles/shc_vm/tasks/main.yml jinja[invalid]
