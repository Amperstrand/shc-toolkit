---
# Converge playbook for the caddy molecule scenario.
# Role is referenced by name (not path) — ANSIBLE_ROLES_PATH is set in
# molecule.yml to point at ansible/roles/.

- name: Converge — install and configure Caddy
  hosts: all
  become: true
  gather_facts: true
  roles:
    - caddy
